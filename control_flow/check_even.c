#include <stdio.h>

/* write a program that checks if the integer given is even or odd */
int main() {
    int num = 3;
    if (num % 2 == 0) {
        printf("Even\n");
    } else {
        printf("Odd\n");
    }
    return 0;
}
